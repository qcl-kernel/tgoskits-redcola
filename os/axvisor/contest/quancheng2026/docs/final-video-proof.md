# Final Video Proof

This note separates the previously generated draft video from the selected
user-narrated final delivery. The draft remains useful as a visual reference,
but it is not the authoritative 2026-08-24 upload.

## Previous Draft Video (Superseded)

| Item | Value |
| --- | --- |
| Package path | `video/redcola-axvisor-demo.mp4` |
| Container | `mov,mp4,m4a,3gp,3g2,mj2` |
| Duration | `300.066667` seconds |
| Size | `22811577` bytes |
| Video | H.264, `1920x1080`, `30 fps` |
| Audio | AAC, `48000 Hz`, stereo |
| SHA256 | `909bdc9f39527404ac12cccd9515caaf255be649a0de8e454c8541e297201bba` |

This draft passed media checks, but it is superseded because the selected final
workflow uses the reviewed PPT and narration script with the team leader's own
voice.

## Final User-Narrated Video

| Item | Value |
| --- | --- |
| Status | `PENDING RECORDING` |
| Package path | `video/redcola-axvisor-demo.mp4` |
| Duration gate | `240..360` seconds |
| Required streams | at least one video stream and one narration audio stream |
| Metadata and SHA256 | generated by `scripts/verify_demo_video.py` |
| Packaged metadata | `video/METADATA.txt` |
| Package gate | strict build, hash verification and fresh-unzip verification by `scripts/finalize_final_submission.sh` |

## Visible Evidence

The final presentation has ten scoring chapters. It shows the architecture,
resource isolation, merged AxVisor support, the 30000-sample task-one run,
before/after and native-RTOS comparisons, QCZ1 reliability, the AI control
loop, StarryOS bonus evidence and final delivery gates.

The eighth chapter is an animated two-terminal playback derived from the same
latest-`dev` QEMU evidence used by the submitted summaries. The left terminal
shows Zephyr/RTOS UDP receive and control output; the right terminal shows the
two-vCPU Linux guest, UDP/QCZ1 requests, AI inference, ACK/STATUS replies and
the final PASS markers. This makes the cross-guest interaction visible without
claiming that a synthetic slide is a new runtime measurement.

The superseded draft media checks completed on 2026-08-21:

```text
10/10 chapter snapshots present
blackdetect: no black interval >= 1 second
silencedetect: no silence interval >= 8 seconds
FINAL_VIDEO_DUAL_TERMINAL_BUILD=PASS
```

## Review Boundary

Only output from `scripts/verify_demo_video.py` for the user-narrated MP4 and
the final archive `.sha256` sidecar may be treated as final metadata. Do not
reuse the draft hash above for the 2026-08-24 upload.

The video content should be checked against:

```text
docs/final-demo-acceptance-checklist.md
docs/demo-video-script.md
docs/final-video-cue-card-cn.md
docs/final-demo-recording-runbook.md
```

The final uploaded ZIP identity is still the generated package `README.txt`
plus the sibling external `.zip.sha256` file. This video hash is only the hash
of the packaged MP4 file.
